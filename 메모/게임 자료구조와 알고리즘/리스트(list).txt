리스트

특징
1. 노드 기반의 컨테이너
2. 이중 연결 리스트(double linked list) 기반 -> 하지만 c++에서 기본적으로 구현되어 있음
3. 인덱스 접근 연산자를 지원하지 않는다->탐색위치를 특정해서 바로 찾아낼수 없다
4. 다른 list와 결합할 때 좋은 컨테이너 -> list끼리 유기적으로 결합 가능
5. 순차열 중간에 삽입,제거에 유리함


사용방법

#include<list>
using namespace std;

void main()
{
list<int> lt;
}
