디파인 값을 보기좋게 모아서 사용하는 방식

시작값은 따로 설정하지 않으면 0부터 시작하며 그 다음은 자동으로 1씩 증가

enum을 변수처럼 사용할시 해당 enum범위 안에 있는 변수'만'으로(a=0일때 a로만 사용가능하고 0은 사용불가) 사용가능

ex)
enum status
{
	a,
	b,
	c
};


main()
{
status A=a;

status A=0;    >> 상수 값은 오류남


}
