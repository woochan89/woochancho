typeid

특징

1. 자료형이나 변수 또는 식을 입력받아 const type_info& 형식의 객체를 반환해주는 연산자
2. 자료형(int), 클래스, 함수, 객체, enum, 사용자 정의 클래스, 멤버 함수 등을 받을수 있는 연산자





type_info

특징


ex)
#include <iostream>
using namespace std;
void main(){
cout<< typeid(int).name() << endl;
cout<< typeid(type_info).name() << endl;
cout<< typeid(main).name() << endl;
cout<< typeid(cout).name() << endl;
}

출력결과

int 
class type_info
...