* 어떤 메모리의 시작점 부터 연속된 범위를 어떤 값으로(바이트 단위)모두 지정하고 싶을 때 사용

* 1바이트 마다 채워짐

* for문으로 채우는것보다 더 빠른 속도가 나올수 있다(컴파일러 또는 컴퓨터 여건에 따라 다름)

ex)
memset(arr,0,sizeof(char)len+1);
arr : 채우고자 하는 메모리의 시작 포인터
0 : 메모리에 채우고자 하는 값
sizeof(char)len+1 : 채우고자 하는 메모리의 크기